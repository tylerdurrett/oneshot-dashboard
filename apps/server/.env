# Docker sandbox configuration
# These values are used by the agent server to communicate with the Docker sandbox.
# Override in .env.local for local development if needed.

# Name of the Docker sandbox to connect to
SANDBOX_NAME=my-sandbox

# Working directory path inside the Docker sandbox
SANDBOX_WORKSPACE=/workspace
