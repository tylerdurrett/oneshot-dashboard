# Database connection (default: file:local.db)
# DATABASE_URL=file:local.db

# Note: Dev server port is configured via `pnpm hello` and stored in project.config.json
# (not in .env.local). See AGENTS.md for details.
