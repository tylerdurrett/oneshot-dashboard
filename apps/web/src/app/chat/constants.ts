/**
 * Placeholder thread ID used until thread management is wired up (Phase 5.4 / 6.x).
 * Search for this constant to find all call sites that need updating.
 */
export const PLACEHOLDER_THREAD_ID = 'placeholder-thread';
